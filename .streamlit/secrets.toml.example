# Copy to secrets.toml for local dev (or paste into Streamlit Cloud Advanced settings)

# Optional: restrict instructor pages to people who know this PIN
instructor_pin = "laos2026"

# Leave blank to use local SQLite + filesystem (great for testing without Supabase)
# supabase_url = "https://YOUR_PROJECT.supabase.co"
# supabase_key = "YOUR_ANON_KEY"
